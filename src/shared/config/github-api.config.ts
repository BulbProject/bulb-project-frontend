const gitHubApiConfig = {
  baseUrl: 'https://udoc.eprocurement.systems/entries/BulbProject/bulb-project-frontend',
  // Process.env.APP_ENV === 'STAGE' ? 'master' : 'develop',
  branch: 'tutorial',
};

export default gitHubApiConfig;
