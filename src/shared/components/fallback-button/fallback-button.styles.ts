import styled from 'styled-components';
import Button from 'ustudio-ui/components/Button';

const FallbackButton = styled(Button)`
  margin-top: var(--i-large);
`;

const Styled = { FallbackButton };

export default Styled;
