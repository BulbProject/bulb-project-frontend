import styled from 'styled-components';

const SelectContainer = styled.div`
  position: relative;
  select {
    cursor: pointer;

    background-color: #f5f5f5;
    color: black;

    padding: 5px;
    appearance: auto;
  }
`;

const Styled = { SelectContainer };

export default Styled;
