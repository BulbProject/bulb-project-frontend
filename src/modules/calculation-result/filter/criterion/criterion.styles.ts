import styled from 'styled-components';

const Divider = styled.hr`
  height: 1px;
  width: 100%;

  margin: var(--i-regular) 0;

  background-color: var(--c-light);
`;

const Styled = { Divider };

export default Styled;
