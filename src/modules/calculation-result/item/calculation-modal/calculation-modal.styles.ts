import styled from 'styled-components';
import Flex from 'ustudio-ui/components/Flex';

const WarningContainer = styled(Flex)`
  height: 5px;
  margin-bottom: 20px;
`;

const Styled = { WarningContainer };

export default Styled;
